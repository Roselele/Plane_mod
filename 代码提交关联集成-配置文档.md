---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: 'c492d4c6-a199-4997-8a9c-79aa9cda2a70'
  PropagateID: 'c492d4c6-a199-4997-8a9c-79aa9cda2a70'
  ReservedCode1: 'edb25f1f-bd01-49d0-9e70-8ea574901e6c'
  ReservedCode2: 'edb25f1f-bd01-49d0-9e70-8ea574901e6c'
---

# 代码提交关联集成 — 配置文档（GitLab + GitHub）

## 功能概述

本集成通过 GitLab / GitHub Webhook 自动将代码提交（Commit）、合并请求 / PR 和评论关联到 Plane 工作项（Issue）。支持三种关联方式：

1. **Commit Message 引用**：在提交消息中写 `#42` 或 `PROJECT1-42`
2. **分支名自动关联**：用包含工作项编号的分支名（如 `PROJECT1-1/fix-login`），推送时自动关联，无需在 commit message 中写引用
3. **手动贴 URL**：在工作项详情页 Links 标签页直接贴 commit / PR 链接

支持同时接入 GitLab 和 GitHub，两个平台共用同一套引用解析逻辑，各自独立配置 Secret 验证。

### 工作流程

```
GitLab / GitHub 仓库 → (push/MR(PR)/comment 事件) → Plane Webhook 端点
  → 解析 commit message + 分支名 → 匹配 Issue 引用 → 创建 IssueLink
  → 工作项详情页自动展示链接
```

---

## 1. 环境配置

在项目根目录 `.env` 文件中设置：

```bash
# GitLab Incoming Webhook
# 设置一个密钥令牌，与 GitLab webhook 配置中的 Secret Token 保持一致
# 留空则接受所有请求（仅限开发环境，生产环境必须设置）
GITLAB_WEBHOOK_SECRET="your-gitlab-secret-token"

# GitHub Incoming Webhook
# 设置一个密钥令牌，与 GitHub webhook 配置中的 Secret 保持一致
# GitHub 使用 HMAC-SHA256 签名验证，留空则接受所有请求（仅限开发环境）
GITHUB_WEBHOOK_SECRET="your-github-secret-token"
```

修改 `.env` 后需要重启 API 容器：

```powershell
docker restart plane-api-1
```

---

## 2. GitLab Webhook 配置

### 2.1 获取 Webhook URL

Webhook URL 格式：

```
http(s)://<your-plane-domain>/api/workspaces/<workspace-slug>/integrations/gitlab/webhook/
```

**示例**：如果 Plane 运行在 `http://localhost:8000`，workspace slug 为 `ws1`：

```
http://localhost:8000/api/workspaces/ws1/integrations/gitlab/webhook/
```

### 2.2 在 GitLab 中配置 Webhook

1. 打开 GitLab 仓库 → **Settings** → **Webhooks**
2. 在 **URL** 中填入上面的 Webhook URL
3. 在 **Secret token** 中填入 `.env` 里设置的 `GITLAB_WEBHOOK_SECRET` 值
4. 在 **Trigger** 中勾选以下事件：
   - ✅ Push events（代码推送）
   - ✅ Merge request events（合并请求）
   - ✅ Comments / Note events（评论，可选）
5. 点击 **Add webhook**
6. 点击 **Test** → 选择 **Push events** 发送测试请求，确认返回 `200 OK`

> **极狐 GitLab (gitlab.cn)** 的配置方式与 GitLab 完全一致，仅仓库 URL 域名不同。

---

## 3. GitHub Webhook 配置

### 3.1 获取 Webhook URL

Webhook URL 格式：

```
http(s)://<your-plane-domain>/api/workspaces/<workspace-slug>/integrations/github/webhook/
```

**示例**：如果 Plane 运行在 `http://localhost:8000`，workspace slug 为 `ws1`：

```
http://localhost:8000/api/workspaces/ws1/integrations/github/webhook/
```

### 3.2 在 GitHub 中配置 Webhook

1. 打开 GitHub 仓库 → **Settings** → **Webhooks** → **Add webhook**
2. 在 **Payload URL** 中填入上面的 Webhook URL
3. 在 **Content type** 选择 `application/json`
4. 在 **Secret** 中填入 `.env` 里设置的 `GITHUB_WEBHOOK_SECRET` 值
5. 在 **Which events would you like to trigger this webhook** 中选择：
   - 选择 **Let me select individual events**
   - ✅ Push（代码推送）
   - ✅ Pull requests（PR 事件）
   - ✅ Issue comments（评论，可选）
6. 确保 **Active** 勾选
7. 点击 **Add webhook**
8. GitHub 会自动发送 ping 事件，确认 Recent Deliveries 显示绿色 ✓（返回 `200 OK`）

### 3.3 验证签名差异

| 平台    | 验证方式          | 请求头                   | 密钥传递方式            |
| ------- | ----------------- | ------------------------ | ----------------------- |
| GitLab  | 明文比对          | `X-Gitlab-Token`         | 直接在 header 中传明文  |
| GitHub  | HMAC-SHA256 签名  | `X-Hub-Signature-256`    | 用 Secret 对 body 签名  |

---

## 4. 多项目/多仓库配置

- 每个 GitLab / GitHub 仓库可以独立配置 webhook，指向同一个 Plane workspace
- 不同 workspace 使用各自的 webhook URL（slug 不同）
- 同一个仓库可以同时推送到多个 Plane workspace（配置多个 webhook）
- GitLab 和 GitHub 可以同时接入同一个 Plane workspace，互相不冲突
- 链接的 metadata 中通过 `source` 字段（`"gitlab"` / `"github"`）区分来源

---

## 5. 关联方式

GitLab 和 GitHub 支持完全相同的关联方式。共有三种途径，可以组合使用：

### 方式一：分支名自动关联（推荐）

用包含工作项编号的分支名，推送时自动关联，**commit message 中不需要写任何引用**。这是最直观的方式——建分支时带上编号，后续所有提交自动关联。

**格式 A：`项目标识符-序号/描述`（精确匹配，推荐）**

```
# 分支名示例
PROJECT1-1/fix-login-bug
PROJECT1-2/add-payment
guyu-3/refactor-shader
```

- 先匹配项目标识符（如 `PROJECT1`），再匹配序号（如 `1`）
- 标识符不区分大小写（`project1-1/...` 也可以）
- 一条分支名中只有一个编号会生效（匹配到的第一个）

**格式 B：`序号/描述`（跨项目搜索）**

```
# 分支名示例
1/hotfix-critical
2/feature-dark-mode
```

- 在当前 workspace 的所有项目中查找对应序号的工作项
- 适合 workspace 内只有一个项目的场景

**分支名规则**：
- 编号必须在分支名的开头，或者在 `/` 后面
- push 事件解析推送的分支名（`ref` 字段）
- MR/PR 事件解析源分支名（source branch / head ref）
- 分支名引用与 commit message 引用会合并去重

### 方式二：Commit Message 引用

在提交消息、MR/PR 描述或评论中引用 Plane 工作项，支持两种格式：

#### 格式一：`#序号`（跨项目搜索）

```
git commit -m "修复登录页面 bug #42"
```

- 在当前 workspace 的所有项目中查找 `sequence_id = 42` 的工作项
- 如果多个项目存在相同序号的工作项，取第一个匹配
- 适合 workspace 内只有一个项目的场景

#### 格式二：`项目标识符-序号`（精确匹配）

```
git commit -m "修复登录页面 bug PROJECT1-42"
```

- 先找到 identifier 为 `PROJECT1` 的项目，再匹配 `sequence_id = 42`
- 精确到具体项目，推荐使用
- 标识符不区分大小写（Plane 会自动转大写），但 commit message 中建议用大写

#### 格式对照表

| 写法            | 含义                          | 搜索范围                |
| --------------- | ----------------------------- | ----------------------- |
| `#42`         | 序号为 42 的工作项            | 当前 workspace 所有项目 |
| `PROJECT1-42` | PROJECT1 项目的第 42 号工作项 | 仅 PROJECT1 项目        |

#### 实际示例

```
# 同时引用多个工作项
git commit -m "Refactor auth module, resolve #1 and PROJECT1-2"

# MR/PR 标题和描述中都可以引用
MR 标题:  "Fix login bug #1"
PR 标题:  "Fix login bug #1"
PR 描述:  "This PR resolves #1 and PROJECT1-2"

# 评论中引用
评论内容: "This is related to #2"
```

### 方式三：手动贴 URL

在工作项详情页 → **Links** 标签页中点击“添加链接”，直接粘贴 commit / MR/PR 的 URL。零配置，但需要手动操作。

---

## 6. 支持的事件类型

| 事件          | GitLab Trigger       | GitHub Event         | 解析内容                            | 生成链接 URL      |
| ------------- | -------------------- | -------------------- | ----------------------------------- | ----------------- |
| Push          | Push events          | Push                 | 每个 commit 的 message + **分支名** | Commit URL        |
| MR / PR       | Merge request events | Pull request         | MR/PR 标题 + 描述 + **源分支名**    | MR/PR URL         |
| Comment       | Comment events       | Issue comment        | 评论内容                            | 评论 URL          |
| Ping          | Webhook 配置测试     | Ping（自动发送）     | —                                   | —（仅返回 pong）  |

### Commit URL 构造方式差异

| 平台    | Commit URL 来源                                                    |
| ------- | ----------------------------------------------------------------- |
| GitLab  | payload 中直接提供 `commits[].url`                                |
| GitHub  | 需拼接 `repository.html_url` + `/commit/` + `commits[].id`(SHA)  |

---

## 7. 工作项链接展示

链接创建后，在 Plane 的工作项详情页 → **Links** 标签页中自动展示：

- **标题**：Commit message 首行 / MR/PR 标题 / 评论首行
- **URL**：GitLab/GitHub commit / MR/PR / 评论的链接
- **图标**：默认链接图标（如仓库为公网可访问，Celery 爬虫任务可能自动更新 favicon）
- **来源标记**：metadata 中记录来源信息

### Metadata 结构

```json
{
  "source": "gitlab",       // "gitlab" 或 "github"
  "event_type": "push",     // push / merge_request / pull_request / note / issue_comment
  "repo": "test-repo",
  "sha": "abc123d",         // commit SHA 前7位（仅 push 事件）
  "author": "Test User",
  "branch": "PROJECT1-1/fix-login",  // 分支名（仅 push / MR / PR 事件，通过分支名关联时记录）
  "title": "Fix bug #1",
  "url": "https://gitlab.com/test-repo/-/commit/abc123def456",
  "favicon": "data:image/svg+xml;base64,..."
}
```

---

## 8. 去重机制

- 同一个 URL + 同一个工作项 只会创建一条链接（重复触发返回 `already_linked`）
- 同一条 commit message 中多次引用同一个工作项，只创建一条链接
- 不同 commit 的不同 URL 引用同一个工作项，会创建多条链接（正常行为）
- GitLab 和 GitHub 的同一个 URL 不会互相去重（不同平台的 URL 不同）

---

## 9. 安全注意事项

1. **生产环境必须设置 Secret**：
   - GitLab：`GITLAB_WEBHOOK_SECRET`，通过 `X-Gitlab-Token` 头明文比对
   - GitHub：`GITHUB_WEBHOOK_SECRET`，通过 `X-Hub-Signature-256` 头 HMAC-SHA256 签名验证
2. 两个 Secret 独立配置，可以使用不同的值
3. Webhook 端点不需要 Plane 用户认证（`authentication_classes = []`），完全依赖 token/签名验证
4. GitHub 使用 `hmac.compare_digest()` 进行常量时间比较，防止时序攻击
5. 建议将 Webhook URL 设为 HTTPS（生产环境）

---

## 10. 测试与验证

### 10.1 GitLab 快速测试（PowerShell）

```powershell
# 测试 ping 事件
$body = '{"object_kind":"ping"}'
Invoke-RestMethod -Uri "http://localhost:8000/api/workspaces/ws1/integrations/gitlab/webhook/" `
  -Method POST -Body $body -ContentType "application/json"

# 测试 push 事件
$pushBody = @'
{
  "object_kind": "push",
  "ref": "refs/heads/main",
  "project": {"name": "test-repo", "web_url": "https://gitlab.com/test-repo"},
  "commits": [{
    "id": "abc123def456",
    "message": "Fix bug #1",
    "url": "https://gitlab.com/test-repo/-/commit/abc123def456",
    "author": {"name": "Test User"}
  }]
}
'@
Invoke-RestMethod -Uri "http://localhost:8000/api/workspaces/ws1/integrations/gitlab/webhook/" `
  -Method POST -Body $pushBody -ContentType "application/json"
```

### 10.2 GitHub 快速测试（PowerShell）

```powershell
# 测试 ping 事件（需添加 X-GitHub-Event 头）
$headers = @{ "X-GitHub-Event" = "ping" }
Invoke-RestMethod -Uri "http://localhost:8000/api/workspaces/ws1/integrations/github/webhook/" `
  -Method POST -Headers $headers -Body '{}' -ContentType "application/json"

# 测试 push 事件
$pushHeaders = @{ "X-GitHub-Event" = "push" }
$pushBody = @'
{
  "ref": "refs/heads/main",
  "repository": {
    "name": "test-repo",
    "html_url": "https://github.com/test-user/test-repo"
  },
  "commits": [{
    "id": "abc123def456789abcdef0123456789abcdef01",
    "message": "Fix bug #1",
    "author": {"name": "Test User", "username": "testuser"}
  }]
}
'@
Invoke-RestMethod -Uri "http://localhost:8000/api/workspaces/ws1/integrations/github/webhook/" `
  -Method POST -Headers $pushHeaders -Body $pushBody -ContentType "application/json"

# 测试 pull_request 事件
$prHeaders = @{ "X-GitHub-Event" = "pull_request" }
$prBody = @'
{
  "action": "opened",
  "repository": {"name": "test-repo", "html_url": "https://github.com/test-user/test-repo"},
  "pull_request": {
    "title": "Fix login bug #1",
    "body": "This PR resolves PROJECT1-2",
    "html_url": "https://github.com/test-user/test-repo/pull/1"
  },
  "sender": {"login": "testuser"}
}
'@
Invoke-RestMethod -Uri "http://localhost:8000/api/workspaces/ws1/integrations/github/webhook/" `
  -Method POST -Headers $prHeaders -Body $prBody -ContentType "application/json"

# 测试 issue_comment 事件
$commentHeaders = @{ "X-GitHub-Event" = "issue_comment" }
$commentBody = @'
{
  "action": "created",
  "repository": {"name": "test-repo", "html_url": "https://github.com/test-user/test-repo"},
  "comment": {
    "body": "This is related to #2",
    "html_url": "https://github.com/test-user/test-repo/issues/2#issuecomment-1"
  },
  "sender": {"login": "testuser"}
}
'@
Invoke-RestMethod -Uri "http://localhost:8000/api/workspaces/ws1/integrations/github/webhook/" `
  -Method POST -Headers $commentHeaders -Body $commentBody -ContentType "application/json"
```

### 10.3 使用签名验证时的测试

如果设置了 `GITHUB_WEBHOOK_SECRET`，需要计算 HMAC 签名：

```powershell
$secret = "your-github-secret-token"
$body = '{"repository":{"name":"test-repo","html_url":"https://github.com/test-user/test-repo"},"commits":[{"id":"abc123","message":"Fix bug #1","author":{"name":"Test"}}]}'
$bytes = [System.Text.Encoding]::UTF8.GetBytes($body)
$hmac = New-Object System.Security.Cryptography.HMACSHA256
$hmac.Key = [System.Text.Encoding]::UTF8.GetBytes($secret)
$hash = $hmac.ComputeHash($bytes)
$signature = "sha256=" + [System.BitConverter]::ToString($hash).Replace("-","").ToLower()
$headers = @{ "X-GitHub-Event" = "push"; "X-Hub-Signature-256" = $signature }
Invoke-RestMethod -Uri "http://localhost:8000/api/workspaces/ws1/integrations/github/webhook/" `
  -Method POST -Headers $headers -Body $body -ContentType "application/json"
```

### 10.4 返回值说明

| status 值           | 含义                   |
| ------------------- | ---------------------- |
| `linked`          | 成功创建链接           |
| `already_linked`  | 链接已存在（去重跳过） |
| `issue_not_found` | 引用的工作项不存在     |
| `error`           | 创建链接时出错         |

### 10.5 验证链接创建

在 Plane 界面中打开对应工作项 → 查看 **Links** 标签页，应能看到自动创建的链接。

---

## 11. 内网穿透（ngrok）

本地开发时，GitLab / GitHub 无法直接访问 `localhost`，需要用 ngrok 将本地端口暴露到公网。

### 11.1 启动 ngrok

```powershell
# ngrok 可执行文件位置
G:\project\plane\.temp\ngrok\ngrok.exe http 8000
```

### 11.2 使用 ngrok URL

将 ngrok 给出的 URL 替换 Webhook URL 中的域名：

```
https://<ngrok-subdomain>.ngrok-free.dev/api/workspaces/ws1/integrations/gitlab/webhook/
https://<ngrok-subdomain>.ngrok-free.dev/api/workspaces/ws1/integrations/github/webhook/
```

> **注意**：ngrok 免费版会拦截请求显示警告页，需要添加 `ngrok-skip-browser-warning: true` header 绕过。但由于 GitLab/GitHub webhook 发送时不带此 header，可能需要使用付费版或自建隧道方案。

---

## 12. 技术架构

### 文件清单

| 文件                                                         | 用途                                                |
| ------------------------------------------------------------ | --------------------------------------------------- |
| `apps/api/plane/app/views/integrations/base.py`            | **共享逻辑**（正则匹配、issue解析、IssueLink创建）  |
| `apps/api/plane/app/views/integrations/gitlab.py`          | GitLab webhook 视图（payload解析 + Token验证）      |
| `apps/api/plane/app/views/integrations/github.py`          | GitHub webhook 视图（payload解析 + HMAC签名验证）   |
| `apps/api/plane/app/views/integrations/__init__.py`        | Python 包初始化                                     |
| `apps/api/plane/app/urls/integration.py`                   | URL 路由定义（GitLab + GitHub 两个端点）            |
| `apps/api/plane/app/views/__init__.py`                     | 视图导入注册                                        |
| `apps/api/plane/app/urls/__init__.py`                      | 路由汇总注册                                        |
| `.env`                                                      | 环境变量配置                                        |

### 架构设计

```
                    ┌─────────────┐
                    │   base.py   │  ← 共享逻辑
                    │  find_      │
                    │  references │
                    │  resolve_   │
                    │  issue      │
                    │  create_    │
                    │  issue_link │
                    │  process_   │
                    │  references │
                    └──────┬──────┘
                           │
              ┌────────────┴────────────┐
              │                         │
    ┌─────────┴─────────┐     ┌─────────┴─────────┐
    │    gitlab.py      │     │    github.py      │
    │  payload 解析     │     │  payload 解析     │
    │  X-Gitlab-Token   │     │  HMAC-SHA256      │
    │  明文验证         │     │  签名验证         │
    └─────────┬─────────┘     └─────────┬─────────┘
              │                         │
              ▼                         ▼
    POST /gitlab/webhook/     POST /github/webhook/
```

### API 端点

| 端点 | 方法 | 认证 | 用途 |
|------|------|------|------|
| `/api/workspaces/<slug>/integrations/gitlab/webhook/` | POST | X-Gitlab-Token | GitLab incoming webhook |
| `/api/workspaces/<slug>/integrations/github/webhook/` | POST | X-Hub-Signature-256 | GitHub incoming webhook |

- **请求体**：对应平台的 webhook payload（JSON）
- **响应**：200 OK + 处理结果摘要
- **幂等性**：重复发送相同事件不会创建重复链接

### 复用现有机制

- **IssueLink 模型**：直接复用 Plane 内置的工作项链接模型
- **前端展示**：无需额外前端改动，链接自动出现在工作项详情的 Links 标签页
- **Soft Delete**：IssueLink 继承 SoftDeleteModel，支持软删除

---

## 13. 常见问题

### Q: GitLab 显示 webhook 返回 403

A: `X-Gitlab-Token` 头与 `.env` 中的 `GITLAB_WEBHOOK_SECRET` 不匹配。确认两边值一致。

### Q: GitHub 显示 webhook 返回 403

A: `X-Hub-Signature-256` 签名验证失败。检查：
1. `.env` 中 `GITHUB_WEBHOOK_SECRET` 与 GitHub webhook 配置中的 Secret 完全一致
2. 确保 GitHub webhook 的 Content type 设置为 `application/json`（不是 `application/x-www-form-urlencoded`）
3. 确认没有多余空格或换行

### Q: 链接没有自动创建

A: 检查以下几点：

1. commit message 中是否包含正确格式的引用（`#42` 或 `PROJECT1-42`）
2. 工作项是否存在（检查返回的 `status` 是否为 `issue_not_found`）
3. workspace slug 是否正确
4. API 容器是否正常运行
5. GitHub 事件是否在 supported 列表中（push / pull_request / issue_comment）
6. GitLab 事件是否在 supported 列表中（push / merge_request / note）

### Q: 内网 GitLab 的链接没有 favicon

A: Plane 的 Celery 爬虫任务有 SSRF 保护，无法访问内网地址。链接仍会正常创建和展示，只是没有网站图标。标题使用 commit message 首行，不影响使用。

### Q: GitLab 和 GitHub 可以同时使用吗

A: 可以。两个平台的 webhook 端点完全独立，互不影响。同一个工作项可以同时被 GitLab commit 和 GitHub PR 关联，链接会分别创建，通过 metadata 中的 `source` 字段区分来源。

### Q: 如何删除自动创建的链接

A: 在 Plane 界面的工作项详情 → Links 标签页中手动删除，或通过 API 调用 DELETE 接口。

### Q: ngrok 免费版导致 webhook 失败怎么办

A: ngrok 免费版会返回拦截页面。解决方案：
1. 使用 ngrok 付费版
2. 使用其他内网穿透工具（如 frp、Cloudflare Tunnel）
3. 部署到公网服务器
4. 本地测试时去掉 Secret 验证（仅开发环境）

> AI生成